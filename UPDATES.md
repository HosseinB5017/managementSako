# بروزرسانی سیستم احراز هویت

## تغییرات انجام شده بر اساس درخواست:

### 🔄 کامپوننت‌های جدید

#### 1. **OTPInput** (`/components/otp-input.tsx`)
- پشتیبانی از اعداد فارسی و انگلیسی
- طول متغیر (پیش‌فرض 4 رقم، قابل تنظیم)
- عملکرد چند کادره با فوکوس خودکار
- پشتیبانی از paste
- حذف هوشمند (backspace به کادر قبلی می‌رود)
- جهت‌یابی LTR برای نمایش بهتر اعداد

#### 2. **NumericInput** (`/components/numeric-input.tsx`)  
- تبدیل خودکار اعداد فارسی (۱۲۳) به انگلیسی (123)
- نمایش اعداد فارسی در حالت RTL
- محدودیت طول ورودی
- پشتیبانی از paste
- فیلتر کردن کاراکترهای غیر عددی

### 📝 فرم ثبت نام جدید

بر اساس تصویر ارسالی، فرم شامل موارد زیر است:
- ✅ نام و نام خانوادگی (الزامی)
- ✅ تحصیلات (اختیاری) - dropdown
- ✅ سال تولد (اختیاری) - عددی فارسی
- ✅ تعداد اعضای خانواده (اختیاری) - عددی فارسی  
- ✅ شماره تلفن (غیرقابل تغییر)
- ✅ شهر (غیرقابل تغییر - مشهد)
- ❌ آدرس و کد ملی حذف شدند

### 🎨 بهبودهای UI/UX

1. **ورودی شماره تلفن**:
   - نمایش اعداد فارسی در حالت RTL
   - تبدیل خودکار به انگلیسی در backend

2. **ورودی OTP**:
   - طراحی 4 کادر جداگانه
   - انیمیشن و تغییر رنگ هنگام پر شدن
   - حرکت خودکار بین کادرها

3. **فرم ثبت نام**:
   - دکمه بازگشت در بالای فرم
   - فیلدهای غیرقابل تغییر با رنگ خاکستری
   - چیدمان بهتر فیلدها

### 🔧 تغییرات فنی

1. **API Updates**:
   - حذف validation کد ملی از `register.ts`
   - حذف فیلدهای آدرس، استان
   - اضافه کردن فیلدهای جدید

2. **State Management**:
   - بروزرسانی نوع داده‌ها در formData
   - حذف متغیرهای غیرضروری

3. **Type Safety**:
   - رفع خطاهای TypeScript
   - بهبود type definitions

## نحوه استفاده از کامپوننت‌ها:

### OTPInput:
```tsx
<OTPInput
  length={4}              // تعداد کادرها (پیش‌فرض: 4)
  value={otp}             // مقدار فعلی
  onChange={setOtp}       // تابع تغییر
  onComplete={handleComplete} // تابع کامل شدن (اختیاری)
  disabled={isLoading}    // غیرفعال کردن (اختیاری)
/>
```

### NumericInput:
```tsx
<NumericInput
  value={phone}           // مقدار انگلیسی
  onChange={setPhone}     // تابع تغییر (انگلیسی)
  maxLength={11}          // حداکثر طول
  dir="rtl"              // جهت نمایش (فارسی)
  placeholder="۰۹۱۲..."   // متن راهنما
  inputMode="tel"        // نوع کیبورد
/>
```

---

**نتیجه**: سیستم احراز هویت حالا کاملاً فارسی‌سازی شده و با UI/UX بهتری عمل می‌کند. کاربران می‌توانند اعداد فارسی تایپ کنند و سیستم آن‌ها را به درستی پردازش می‌کند.